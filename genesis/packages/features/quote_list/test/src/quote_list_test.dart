// ignore_for_file: prefer_const_constructors

import 'package:flutter_test/flutter_test.dart';
import 'package:quote_list/quote_list.dart';

void main() {
  group('QuoteList', () {
    test('can be instantiated', () {
      expect(QuoteListScreen(), isNotNull);
    });
  });
}
